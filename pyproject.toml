[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "dravidian-lm"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "accelerate",
    "datasets",
    "evaluate",
    "numpy",
    "seqeval",
    "sentencepiece",
    "torch",
    "transformers",
    "tqdm",
    "PyYAML",
]

[tool.setuptools.packages.find]
where = ["src"]
